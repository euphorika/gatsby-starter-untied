{
  "private": true,
  "workspaces": [
    "gatsby-theme-untied",
    "site"
  ],
  "scripts": {
    "predeploy": "yarn workspace gatsby-theme-untied test",
    "deploy": "yarn workspace site build",
    "clean": "yarn workspace gatsby-theme-untied clean && yarn workspace site clean",
    "format": "yarn workspace gatsby-theme-untied format && yarn workspace site format"
  }
}
